# FoodLink Client

This is the prototype client built with React and Vite.

## Setup
1. Clone the repository:
   ```bash
   git clone <your-repo-link>
   cd foodlink-client

2. Install Dependencies
npm install

3. Run the development server:
npm run dev

4. Open the local link in the terminal (most likely http://localhost:5173

